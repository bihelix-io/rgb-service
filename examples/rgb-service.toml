[service]
bind = "127.0.0.1:8787"
network = "regtest"
data_dir = "/tmp/bihelix-rgb-service"
esplora_url = "http://127.0.0.1:3002"

[rna]
issue_fee = 1000
transfer_fee = 100
query_fee = 1

[legacy]
# Disabled by default. This exposes wallet-service-v2 compatible routes for
# trusted callers only.
enabled = false
allow_loopback = true
allowed_ips = []
sync_timeout_secs = 30
reveal_address_count = 20
# Keep legacy RGB RNA fee collection disabled until the BitPocket path is
# verified end-to-end.
rgb_fee_enabled = false
rgb_fee_collector_address = "bc1q42qxdzefsaqks40j7qtk2u8r00pzxt44nu3qzj"
rgb_fee_contract_id = "rgb:nykNCHhT-BgKdtCi-ilF89kf-JilBhg0-JfInd9k-7MyyYOE"
rgb_fee_amount = 1054000
