# rgb-service-local

Local RGB asset service implementation for BiHelix.

This crate contains RGB20 asset, stock, consignment, validation, and PSBT RGB
commitment logic extracted from `btc-local-wallet`. It does not include
Lightning Network node code and does not own Bitcoin private keys.
