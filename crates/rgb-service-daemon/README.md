# rgb-service-daemon

HTTP daemon for BiHelix RGB service APIs.

This daemon exposes the `rgb-service-api` Axum router. It requires explicit
configuration for service bind address, network, and data directory.
